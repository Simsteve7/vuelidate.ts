{
  "name": "@types/vuelidate",
  "version": "1.0.1",
  "description": "",
  "dependencies": {},
  "license": "MIT",
  "scripts": {
    "build": "tsc --listFiles --listEmittedFiles -p types && echo \"No outputs in external lib mode.\"",
    "lint": "dtslint types"
  }
}
